## How to run:

1. npm install
2. copy paste this line inside a .env.development file "VITE_APP_API_URL=http://localhost:3000"
3. npm run backend
4. npm run dev

### Run Unit Tests

npm run test:unit
